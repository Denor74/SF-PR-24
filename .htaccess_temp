# Проверяем работу .htaccess
# Options +FollowSymLinks
# Redirect /test.html https://ya.ru

# Включаем перенаправление
# Позволяет перезаписывать пути так как нужно
RewriteEngine On
# RewriteCond - 	Определяет условие при котором происходит преобразование
# Определяет доступ: f - читать; d - перезаписывать; l - исключение
# Не применять к существующим файлам файлам
# Эти директивы задают условия для следующей после них директивы RewriteRule.
# !-f требует, чтобы правило RewriteRule применялось только в том случае, если запросу не соответствует физически существующий файл
# !-d - запрос не должен применяться, если запрашивается физически существующая папка
# правило в RewriteRule, следующее за этими директивами будет касаться только виртуальных URL для которых нет папки или файла на диске сервера
RewriteCond %{REQUEST_FILENAME} !-f
# Не применять к существующим директориям
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-l
# Редирект всех запросов на index.php
# L означает Last, нужен чтобы на этом этапе mod_rewrite сразу остановил работу.
# Короче, небольшое увеличение производительности.
#RewriteRule .* index.php [L]
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]